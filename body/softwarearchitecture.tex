\chapter{Software Architecture Design}
% \label{chap:software-architecture-design}
This chapter outlines the architectural framework of the KU Parking system. It establishes the Domain Model to define core business entities and the Design Class Diagram to illustrate the static structure and modularity of the mobile and web applications. Furthermore, it details the integration of the AI Component and the Intersection over Union (IoU) algorithm, explaining how computer vision data flows through the model.

\section{Domain Model}
\label{section:domain-model}

\subsection{Mobile Application Domain Diagram}
\label{subsection:mobile-application-domain-diagram}
The domain model diagram in Figure \ref{fig:mobile-application-domain-diagram} delineates the core conceptual entities within the KU Parking application and their relational logic, abstracting away platform-specific implementation details to focus on fundamental business rules. The system revolves around the central "ParkingSpot" entity, which tracks key information such as its name, capacity, availability, and visual status indication. The primary actor, the "User", interacts with the domain through three main interactions: searching for parking areas, submitting an "IssueReport" to flag inaccuracies like closed lots or incorrect availability, and generating a "NavigationRoute" to accurately reach a chosen destination. Both the user's current position and the parking spots themselves are spatially defined through composition with a "Location" value object holding geographic coordinates. Ultimately, this structural representation maps the conceptual flow of user-driven actions as they query, navigate towards, and evaluate physical parking locations in the system.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{diagrams/mobile-app/domain_diagram.png}
    \caption{Domain diagram illustrating the key concepts and relationships in the KU Parking mobile application.}
\label{fig:mobile-application-domain-diagram}
\end{figure}

\subsection{Web Application Domain Diagram}
\label{subsection:web-application-domain-diagram}
The domain model diagram Figure \ref{fig:web-application-domain-diagram} abstracts away technical implementation details to
illustrate the core business concepts and fundamental relationships within the
parking system. At the highest conceptual level, the "ParkingArea" serves as the
aggregate root, defining the overall parking location through essential
properties such as its total capacity and its geographic coordinates via the
"GeoLocation" value object. The aggregate primarily manages two distinct
entities: the "ParkingSpace", representing individual physical stalls whose
spatial boundaries are defined by a "BoundingBox", and the "IssueReport", which
logs anomaly feedback submitted by users. To integrate the model capabilities
purely on a conceptual level, the model introduces the "DetectionResult" value
object. This component encapsulates the inference engine's confidence scores and
object classifications---mapped to its respective "BoundingBox"---which
subsequently determines and updates the occupancy status of each
"ParkingSpace" entity.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{diagrams/web-app/domain_diagram.png}
    \caption{Domain diagram illustrating the key concepts and relationships in the KU Parking web application.}
\label{fig:web-application-domain-diagram}
\end{figure}


\section{Design Class Diagram}
\label{section:design-class-diagram}

\subsection{Mobile Application Class Diagram}
\label{subsection:mobile-application-class-diagram}
The class diagram in Figure \ref{fig:mobile-application-class-diagram} illustrates the component-based architecture of the KU Parking
Mobile App, highlighting the relationships between the primary user interface
modules and the underlying data models. At the core of the system is the 'Index'
component, which serves as the main controller managing the application's state,
including the selected parking spot, current search query, and issue reporting
workflow. The 'Index' component utilizes composition to render modular,
single-purpose child components such as 'ParkingMap', 'SearchBar', 'SpotList',
'SpotDetail', and 'ReportIssue'. Each child component strictly adheres to
defined interface contracts (e.g., 'ParkingMapProps', 'SpotDetailProps') that
specify their required data properties and callback event handlers. Furthermore,
the presentation layer interacts seamlessly with the domain logic by depending
on the shared 'ParkingSpot' entity type and the centralized 'parkingData'
configuration module, ensuring a clear separation of concerns and robust data
consistency across the application.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{diagrams/mobile-app/class_diagram.png}
    \caption{Design class diagram illustrating the structure and relationships of the KU Parking mobile application.}
\label{fig:mobile-application-class-diagram}
\end{figure}

\subsection{Web Application Class Diagram}
\label{subsection:web-application-class-diagram}
This class diagram in Figure \ref{fig:web-application-class-diagram} illustrates the holistic architecture of the parking application, encapsulating both the core domain entities and the integrated AI service components. At the center of the domain is the "ParkingSpot" entity, which acts as an aggregate root containing multiple "Coordinate" objects that define distinct physical parking spaces, alongside associated "IssueReport" entities that track user-submitted feedback. The system distinguishes business logic through dedicated service controllers—namely "ParkingSpotService" and "IssueReportService"—which handle data persistence and API operations. Concurrently, the AI functionality is abstracted through the "AIServiceAPI", which processes video frame inputs and relies on structural interfaces such as "Detection", "BoundingBox", and "SpotOccupancy" to continuously evaluate and update the real-time availability of each parking space.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{diagrams/web-app/web_app_class_diagram.png}
    \caption{Design class diagram illustrating the structure and relationships of the KU Parking web application.}
\label{fig:web-application-class-diagram}
\end{figure}

\section{AI Component}
\label{section:ai-component}

The AI component is a core element of KU Parking. 
It enables parking availability detection by processing streaming video frames from existing surveillance cameras using computer vision techniques. 
The key components include:

\subsection{YOLO-based Vehicle Detection}
YOLO (You Only Look Once) is a real-time object detection algorithm that processes entire images in a single evaluation pass.
YOLO divides images into a grid and simultaneously predicts bounding boxes and class probabilities for detected objects. 
The algorithm's speed and efficiency make it ideal for processing video feeds in near real-time \cite{YOLO}.

\subsection{Parking Space Boundary Detection}
During the initial system setup, parking spot detection model are used to assist in defining parking space boundaries from camera feeds. 
These algorithms detect significant changes in pixel intensity that typically represent lines or boundaries of parking spaces. 
By applying techniques the system can identify straight lines marking parking spaces, 
which are then verified and finalized by system administrators. Once these boundaries are established during setup, 
they remain fixed for ongoing operations, serving as reference coordinates for the IoU calculations to determine parking space occupancy.

\section{Algorithm}
\label{section:algorithm}
\subsection{Intersection over Union (IoU) for Occupancy Detection}
Intersection over Union (IoU) is a crucial metric in the KU Parking system used to determine whether a parking space is occupied. 
The algorithm works by comparing the overlap between two areas: the predefined parking space boundary and the bounding box of any detected vehicle \cite{barseghyan2023parking}.

The IoU calculation is mathematically defined as:

\[
\text{IoU} = \frac{\text{Area of Intersection}}{\text{Area of Union}}
\]

Where:
\begin{itemize}
    \item The intersection is the overlapping region between the parking space boundary and vehicle bounding box
    \item The union is the total combined area of both the parking space boundary and vehicle bounding box
\end{itemize}

The IoU value ranges from 0 to 1, where:
\begin{itemize}
    \item IoU = 0 indicates no overlap (parking space is completely empty)
    \item IoU = 1 indicates perfect overlap (parking space perfectly matches the vehicle dimensions)
\end{itemize}

Our algorithm for determining parking space occupancy follows this process:

\noindent \textbf {For each frame captured by the surveillance camera:}
\begin{enumerate}
    \item Detect Vehicles:
    \begin{itemize}
        \item Apply YOLO to detect vehicles and generate their bounding boxes
        \item Extract the bounding boxes for each detected vehicle.
    \end{itemize}
    \item Evaluate Parking Availability:
    \begin{itemize}
        \item For each predefined parking space boundary:
        \begin{itemize}
            \item Calculate the Intersection over Union (IoU) between the parking space and each detected vehicle's bounding box.
            \item If any IoU value exceeds a predefined threshold, classify the
space as occupied
            \item If no IoU value exceeds the threshold, classify the space as
vacant
        \end{itemize}
    \end{itemize}
\end{enumerate}
    
    % \item Apply temporal filtering to reduce false positives:
    % \begin{itemize}
    %     \item Track occupancy state over multiple consecutive frames
    %     \item Only change occupancy status after consistent detection across several frames
    %     \item This prevents flickering status changes due to temporary occlusions or detection errors
    % \end{itemize}
